#include<iostream>
#include<string>
#include<cstring>
using namespace std;

int main()
{
    string a="/ww";
    cout<<a<<endl;
    a="what";
    a+=a;
    // int i=a.length();
    // while(a[i]!='/'&&i>0)
    //     i--;
    // a.replace(i+1,a.length(),"\0");
    cout<<a<<endl;
    return 0;
}
